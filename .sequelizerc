const path = require("path");

module.exports = {
  config: path.resolve("config", "database.cjs"),
  "models-path": path.resolve("models", "schema"),
  "seeders-path": path.resolve("seeders"),
  "migrations-path": path.resolve("migrations"),
};
